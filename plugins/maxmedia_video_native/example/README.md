# maxmedia_video_native example

Run with `flutter run` on Android, iOS, or macOS. Choose a local video; the app
compresses it with `maxmedia_video_native` and shows byte counts, the temporary output path,
and any warnings. The output is stored in the app's temporary directory, so
copy it elsewhere if you need to keep it.
